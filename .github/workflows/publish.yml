name: Publish to npm and GitHub

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    env:
      # Opt into Node.js 24 for action runtime ahead of GitHub's
      # 2026-06-02 forced default switch. Resolves the deprecation
      # warning for actions/checkout@v4, actions/setup-node@v4 and
      # softprops/action-gh-release@v2, which otherwise run on the
      # deprecated Node 20 (slated for removal 2026-09-16).
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      # Publish to the global npm registry
      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # Publish to GitHub Packages
      - name: Setup Node.js for GitHub Packages
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@a4xrbj1'

      - name: Publish to GitHub Packages
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Create a GitHub Release
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body: |
            See [CHANGELOG.md](https://github.com/a4xrbj1/meteor-desktop/blob/master/CHANGELOG.md) for full release notes.
          generate_release_notes: true
          draft: false
          prerelease: false
